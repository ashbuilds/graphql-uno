type Book {
    id: ID @hashed
    title: String
    description: String
    price: Float
    publishedAt: Date @date #@string
}

type Query {
    book: Book
    books: [Book]
}

type Mutation {
    updateBook(id: ID! @unhashed): ID @hashed
}
